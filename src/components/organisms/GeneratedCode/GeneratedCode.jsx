import React, { useContext } from "react";
import { FormContext } from "../../../context/DataContext";

const GeneratedCode = () => {
  const context = useContext(FormContext);
  return <div>{/* <h1>{context.test}</h1> */}</div>;
};

export default GeneratedCode;
